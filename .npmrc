; VSCode/Claude Code extension host sets NODE_ENV=production in the shell,
; which would cause npm install to silently skip devDependencies.
; Force all dependency types to be installed.
production=false
